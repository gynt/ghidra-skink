#pragma once

{{ '#include "pch.h"' if use_pch }}

namespace Address {
enum {
  {% for entry in addresses -%}
  {% for comment in entry.comments -%}
  // {{ comment }}
  {% endfor -%}
  {{ context.abbreviation }}_{{ context.hash }}_{{ '0x%08X' % entry.address }} = {{ '0x%08X' % entry.address }},
  {% endfor %}
};
}